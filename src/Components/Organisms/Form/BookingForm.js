import PropTypes from "prop-types";

function BookingForm({event, params}) {
    return (
        <div>
            <h2>Event Id is {params.id}</h2>
        </div>
    )
}

export default BookingForm
